'use strict';

module.exports = (bookshelf, connection) => {
  // bookshelf.plugin(require('bookshelf-case-converter-plugin'));
  // bookshelf.plugin(require('bookshelf-camelcase'));
  // bookshelf.plugin('case-converter');
};
